<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidapps.avinashtadavarthy.booktheshow">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/btsicon"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/btsicon"
        android:supportsRtl="true">
        <activity
            android:name=".SplashScreen"
            android:label="Book the Show"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EntryActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SelectCinema"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MovieDetails"
            android:theme="@style/AppTheme3" />
        <activity
            android:name=".SelectSeats"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".SelectFood"
            android:theme="@style/AppTheme3"/>
    </application>

</manifest>